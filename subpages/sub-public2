<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>Sub Public2</title>
  <style>
    body { font-family: sans-serif; padding: 20px; background: #f0f0f0; }
    .form-item {
      background: #fff; padding: 15px; margin-bottom: 10px;
      border: 1px solid #ccc; border-radius: 8px;
    }
    .form-item a { color: blue; text-decoration: underline; }
  </style>
</head>
<body>
  <h1>세무서/사업서식</h1>
  <div id="form-list">불러오는 중...</div>

  <script>
    const jsonFile = "data/sub-public2.json";

    fetch(jsonFile)
      .then(response => response.json())
      .then(data => {
        const container = document.getElementById("form-list");
        container.innerHTML = "";
        data.forEach(item => {
          const div = document.createElement("div");
          div.className = "form-item";
          div.innerHTML = `
            <strong>${item.code}</strong> - ${item.title}<br>
            설명: ${item.description}<br>
            <a href="${item.pdf}" target="_blank">📄 PDF 보기</a>
          `;
          container.appendChild(div);
        });
      })
      .catch(error => {
        document.getElementById("form-list").innerText = "데이터를 불러올 수 없습니다.";
        console.error("JSON 로딩 실패:", error);
      });
  </script>
</body>
</html>
